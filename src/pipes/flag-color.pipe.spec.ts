import { FlagColorPipe } from './flag-color.pipe';

describe('FlagColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FlagColorPipe();
    expect(pipe).toBeTruthy();
  });
});
